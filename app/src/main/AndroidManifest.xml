<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fsc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/picture"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FSC">
        <activity android:name=".CommentManageActivity"></activity>
        <activity android:name=".AddCommentActivity" />
        <activity android:name=".AddGradeActivity" />
        <activity android:name=".AddActivityActivity" />
        <activity android:name=".AddNoticeActivity" />
        <activity android:name=".DetailOfTheNewsActivity" />
        <activity android:name=".DetailOfTheActivityActivity" />
        <activity android:name=".CommentActivity" />
        <activity android:name=".GradeActivity" />
        <activity android:name=".GradeManageActivity" />
        <activity android:name=".HomeworkActivity" />
        <activity android:name=".HomeworkManageActivity" />
        <activity android:name=".CheckInActivity" />
        <activity android:name=".CheckInManageActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" /> <!-- 需要修改回去，为了测试方便，将顺序改了     现在是对的 -->
        <activity android:name=".MainBeforeLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    </manifest>